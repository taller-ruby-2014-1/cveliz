<h1>Mis Post</h1>
<table class="table table-hover">
	<tr>
		<th>Titulo</th>
		<th>Texto</th>
		<th></th>
		<th></th>
		<th></th>
	</tr>
	<% @posts.each do |post| %>
		<tr>
			<td> <%= post.title %> </td>
			<td> <%= post.text %> </td>
			<td> <%= link_to 'Show', post_path(post)%> </td>
			<td> <%= link_to 'Edit', edit_post_path(post)%> </td>
			<td> <%= link_to 'Delete', post_path(post), method: :delete, data: {confirm: 'Está seguro?'}%> </td>
		</tr>
	<% end %>
</table>

<%= link_to "Nuevo Post", new_post_path, :class=>"btn btn-default btn-lg"%>